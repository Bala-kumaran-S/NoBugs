package com.NoBugs.backend.entity;

/**
 * Defines the possible severity levels for a bug report.
 * Used by both reporters and admins.
 */
public enum Severity {
    CRITICAL,
    HIGH,
    MEDIUM,
    LOW,
    INFORMATIONAL
}
