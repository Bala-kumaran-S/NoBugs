package com.NoBugs.backend.entity;

/**
 * Defines the type of scope for a bug bounty program.
 * Can be public (open to all) or private (invitation-based).
 */
public enum ScopeType {
    PUBLIC,
    PRIVATE
}
